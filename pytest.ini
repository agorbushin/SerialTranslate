[pytest]
# Disable problematic plugins
addopts = -p no:pytest-recording
markers =
    integration: slow tests that call external APIs (set env flags to enable)
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
